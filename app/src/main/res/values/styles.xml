<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="BaseText" parent="AppTheme">
        <item name="android:textSize">18sp</item>
    </style>

    <style name="UnfocusedText" parent="AppTheme">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">#666666</item>
    </style>

    <style name="FocusedText" parent="AppTheme">
        <item name="android:textSize">26sp</item>
        <item name="android:textColor">#000000</item>
    </style>

    <style name="MainCard" parent="AppTheme">
        <item name="android:background">@drawable/main_card_background</item>
        <item name="android:padding">10dp</item>
        <item name="android:layout_marginTop">10dp</item>
    </style>

</resources>
